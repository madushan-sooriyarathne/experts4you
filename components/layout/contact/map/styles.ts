import styled from "styled-components";

const MapBox = styled.section`
  grid-column: full-start / full-end;
  width: 100%;
  height: 90vh;
  margin-top: -10rem;
  margin-bottom: -15rem;
`;

export { MapBox };
